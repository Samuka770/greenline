# Copie este arquivo para `.env` e ajuste o endpoint do formulário

# Opção 1 (recomendada): Aponte o frontend local para o endpoint PUBLICADO
# Substitua pela URL real após o deploy (ex.: https://greenline.vercel.app)
# Isso evita 404 no Vite dev, que não serve rotas /api.
# Exemplo:
# VITE_CONTACT_ENDPOINT=https://SEU_APP.vercel.app/api/send-contact

# Alternativa ao VITE_CONTACT_ENDPOINT: proxy do Vite em dev
# Use se preferir continuar chamando "/api" localmente, mas encaminhar para produção:
# VITE_API_PROXY_TARGET=https://SEU_APP.vercel.app

# Opção 2 (alternativa): usar Formspree
# Crie seu formulário e cole o endpoint aqui. O app usa esta opção
# automaticamente se VITE_CONTACT_ENDPOINT não estiver definido.
VITE_FORMSPREE_ENDPOINT=https://formspree.io/f/SEU_ID_AQUI

# Observação: Não defina variáveis de servidor no .env do cliente.
# Defina no provedor (ex.: Vercel) como SEGREDOS do projeto:
# RESEND_API_KEY=seu_token_resend
# EMAIL_TO=contato@greenline.com
# EMAIL_FROM="Greenline Site <no-reply@greenline.com>"
